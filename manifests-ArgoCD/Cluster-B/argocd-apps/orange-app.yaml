apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orange-cluster-b
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/your-repo.git' # Replace with your URL
    path: manifests-ArgoCD/Cluster-B/overlays/orange
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: orange-namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true