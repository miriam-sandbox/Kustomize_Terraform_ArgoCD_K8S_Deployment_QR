apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/green

# Adds a prefix or namespace specific to Cluster-A
namespace: green-namespace
namePrefix: cluster-b-

# Built-in image tag management
images:
  - name: argocd-trf-k8s-demo-green
    newName: miriamoranu/argocd-trf-k8s-demo-green
    newTag: v1

patches:
  - target:
      kind: Service
      name: green-service
    patch: |-
      - op: replace
        path: /spec/ports/0/port
        value: 8083
      - op: replace
        path: /spec/type
        value: LoadBalancer